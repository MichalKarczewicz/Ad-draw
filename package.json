{
  "name": "ad-draw",
  "version": "1.0.0",
  "description": "A program in vanilla Javascript that randomizes ads on a page by excluding ads from the page they are currently on.",
  "main": "AdDrawer.js",
  "scripts": {
    "test": "jest"
  },
  "jest": {
    "testEnvironment": "node",
    "transform": {
      "^.+\\.jsx?$": "babel-jest"
    }
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "devDependencies": {
    "@babel/preset-env": "^7.22.10",
    "babel-jest": "^29.6.3",
    "jest": "^29.6.2",
    "jest-environment-jsdom": "^29.6.2",
    "jsdom": "^22.1.0",
    "node-fetch": "^3.3.2",
    "text-encoding": "^0.7.0"
  },
  "directories": {
    "test": "tests"
  },
  "dependencies": {
    "jest-fetch-mock": "^3.0.3"
  }
}
